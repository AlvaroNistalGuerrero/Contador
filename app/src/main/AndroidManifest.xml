<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Contador"
        tools:targetApi="31">

        <activity android:name=".Recycled_vista"
            android:exported="false">

        </activity>

        <activity android:name=".ApuntarMisRecords"
            android:exported="false"/>
        <activity
            android:name=".MenuDeMejoras"
            android:exported="false"
            android:label="MEJORAS"/>
        <activity android:name=".Login"
            android:exported="true"
            android:label="REGISTRATE">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".Opciones"
            android:exported="false" />
        <activity
            android:name=".PantallaInicio"
            android:exported="false"
            android:label="INICIO">

        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="CONTADOR"/>
        <activity
            android:name=".InicioDeSesion"
            android:exported="true"
            android:label="LOGIN"/>

    </application>

</manifest>